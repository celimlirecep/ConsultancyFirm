@model AuthorListViewModel
@{
    ViewData["Title"] = "Authors Page";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-md-6">
        <div class="row">
            @* ************************DANIŞMAN RESİMLERİ KISMI  *@
            @foreach (var author in Model.AuthorInfos)
            {
                <div class="col-md-6">
                    <a href="/authors?id=@author.AuthorId&page=@Model.PageInfo.CurrentPage">
                        <div class="cards-list">

                            <div class="carda ">
                                <div class="card_image"> <img src="@author.Image" /> </div>
                                <div class="card_title title-white">
                                    <p style="background-color:black !important;">@author.FullName</p>
                                </div>
                            </div>

                        </div>
                    </a>


                </div>


            }

        </div>
        @* DANIŞMAN SAYFALAMA *@


    </div>
    <div class="col-md-6 ">
        <div class="text-center">
            <h1>Pisikolog bilgileri</h1>
        </div>
        @if (Model.SelectedAuthor != null)
        {
            <h3>@Model.SelectedAuthor.FullName</h3>
            <h5>Mail Adresi: @Model.SelectedAuthor.Email</h5>
            <h5>Yazar Hakkında: @Model.SelectedAuthor.About</h5>
            <h5>YAZARIN YAZDIĞI YAZILAR EKLENİCEK</h5>
        }
        else
        {
            <div class="text-center">
                <P> DANIŞMANA AİT BİLGİLERİ GÖRMEK İÇİN YAZARI TIKLAYINIZ !!!</P>
            </div>
        }

    </div>

</div>
<nav class="d-flex justify-content-center">
    <ul class="pagination">
        @for (int i = 0; i < Model.PageInfo.TotalPages(); i++)
        {
            <li class="page-item" @(Model.PageInfo.CurrentPage == i + 1 ? "active" : "")>
                <a href="/authors?page=@(i+1)" class="page-link">@(i+1)</a>
            </li>

        }

    </ul>
</nav>




